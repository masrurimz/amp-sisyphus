Use the explorer skill for fast codebase search.

Strategy:
1. **USE SEARCH FIRST** for semantic/conceptual queries (Gemini 3 Flash)
2. **Grep** for exact pattern matching
3. **glob** for file discovery
4. **ALWAYS parallelize** - multiple searches in one message

Output: File paths with line numbers and brief context.
